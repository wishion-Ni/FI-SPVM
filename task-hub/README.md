# Task Hub

该目录用于集中管理 FI-SPVM 的阶段性重构任务清单与执行日志模板。

- 任务清单按批次目录存放（例如 `2026-04-02_v1.0/`）。
- 每个任务文件名与文件头都包含时间与版本号，避免重复和冲突。
- 执行人员应在同批次目录下创建日志文件并持续更新。

建议流程：
1. 先阅读 `00_INDEX_*.md` 了解整体目标与依赖关系。
2. 按序执行 `01_...` 到 `05_...` 任务。
3. 每个任务执行时复制 `LOG_TEMPLATE_*.md` 生成对应日志。
4. 每次提交后更新日志中的“变更摘要/风险/下一步”。
